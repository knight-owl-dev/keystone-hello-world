# Custom Fonts

Place your `.otf` font files in this directory and register them in
`fonts-registry.yaml` to use custom fonts in your Keystone project.

See the comments in `fonts-registry.yaml` for the complete setup guide.
